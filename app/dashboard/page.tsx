// TODO: Build dashboard — design mockup pending.
// Will include: summary cards (income / expenses / net),
// spending donut chart (Recharts), monthly cash flow chart,
// recent transactions list.
export default function DashboardPage() {
  return <div>Dashboard — coming soon</div>
}
